type User {
    id: ID
    name: String
    email: String!
}

type Query {
    getAllUsers: [User]
    getUserById(id: ID!): User
}

type Mutation {
    createUser(name: String!, email: String!): User
}
